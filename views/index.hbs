<h2>Список задач для завершения</h2>

{{#if todos.length}}
<ul>
    {{#each todos}}
        <form action="/complete" method="post">
            <li class="todo">
                <label>
                    {{#if completed}} <!--если true -->
                        <input type="checkbox" checked name="completed">
                        <span class="completed">{{title}}</span>
                    {{else}}
                        <input type="checkbox" name="completed">
                        <span>{{title}}</span>
                    {{/if}}
                    <input type="hidden" value="{{_id}}" name="id">
                    <button class="btn btn-small" type="submit">Сохранить изменения</button>
                </label>
            </li>
        </form>
    {{/each}}
</ul>
{{else}}
    <p>Не найдено! Добавте задачи на страницы добавления списка</p>
{{/if}}
<br>
<h2>Список новостей</h2>
<div class="row">
    {{#if news.length}}
        {{#each news}}
            <div class="col s12 m6 l4">
                <div class="card blue-grey darken-1">
                    <div class="card-content white-text">
                        <span class="card-title">{{title}}</span>
                        <p>{{description}}.</p>
                    </div>
                </div>
            </div>
        {{/each}}
    {{else}}
        <p>Не найдено! Добавте новости</p>
    {{/if}}

</div>